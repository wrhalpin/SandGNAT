__pycache__/
*.py[cod]
*.egg-info/
.venv/
.env
.env.*
!.env.example

.pytest_cache/
.coverage
htmlcov/
dist/
build/

# Local dev artifacts — never commit captured samples or PCAPs
samples/
quarantine/
captures/
*.pcap
*.pml
*.dmp

# Jekyll build output (GitHub Pages builds in CI)
_site/
.jekyll-cache/
Gemfile.lock

# Phase B seed data for guest profile — operators stage local realistic
# content into infra/guest/seed-data/ without committing it.
infra/guest/seed-data/*
!infra/guest/seed-data/README.md
